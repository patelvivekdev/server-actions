/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: 'brtmqwiqrxsubcamdxvy.supabase.co',
      },
    ],
  },
};

export default nextConfig;
